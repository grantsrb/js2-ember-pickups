{{group-display group=model deleteGroup='deleteGroup' updateGroup='updateGroup' makeReview='makeReview'}}
{{#each model.reviews as |review|}}
  {{review-tile review=review deleteReview='deleteReview'}}
{{/each}}


import { skillHelp } from 'js2-ember-pickup-reviews/helpers/skill-help';
import { module, test } from 'qunit';

module('Unit | Helper | skill help');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillHelp([42]);
  assert.ok(result);
});



import { avgFriend } from 'js2-ember-pickup-reviews/helpers/avg-friend';
import { module, test } from 'qunit';

module('Unit | Helper | avg friend');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avgFriend([42]);
  assert.ok(result);
});



import { avgSkill } from 'js2-ember-pickup-reviews/helpers/avg-skill';
import { module, test } from 'qunit';

module('Unit | Helper | avg skill');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avgSkill([42]);
  assert.ok(result);
});


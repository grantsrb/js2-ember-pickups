
import { friendHelp } from 'js2-ember-pickup-reviews/helpers/friend-help';
import { module, test } from 'qunit';

module('Unit | Helper | friend help');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = friendHelp([42]);
  assert.ok(result);
});

